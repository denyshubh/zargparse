{
  "name": "zargparse",
  "version": "1.0.0",
  "description": "zero dependencies argument parser",
  "main": "argparse.js",
  "scripts": {
    "test": "export NODE_ENV=test && (jest || true)"
  },
  "author": "a very smart guy",
  "license": "ISC",
  "devDependencies": {
    "jest": "^24.8.0"
    }
}
